<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="HonestSpend_WinUI.Pages.TaxVaultPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="16" MaxWidth="640">
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Tax vault" />
            <TextBlock Opacity="0.75" TextWrapping="Wrap"
                Text="Virtual reserve that reduces Spendable. Cash stays in your bank — April doesn't ambush checking." />

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="12" Padding="20">
                <StackPanel Spacing="6">
                    <TextBlock Opacity="0.7" Text="Reserved (not spendable)" />
                    <TextBlock x:ConnectionId='13' x:Name="BalanceText" FontSize="36" FontWeight="SemiBold" Text="$0" />
                    <TextBlock x:ConnectionId='14' x:Name="NoteText" Opacity="0.7" TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <CheckBox x:ConnectionId='2' x:Name="EnabledBox" Content="Vault enabled" IsChecked="True" />
            <NumberBox x:ConnectionId='3' x:Name="BalBox" Header="Vault balance ($)" Minimum="0" Value="0" />
            <NumberBox x:ConnectionId='4' x:Name="RateBox" Header="Auto set-aside rate (0–1, optional)" Minimum="0" Maximum="1"
                       PlaceholderText="e.g. 0.25 for 25% of variable income" />
            <Button x:ConnectionId='5' Content="Save vault"                    Style="{StaticResource AccentButtonStyle}" />

            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Quick adjust" />
            <NumberBox x:ConnectionId='6' x:Name="DeltaBox" Header="Delta (+ set aside / − release)" />
            <Button x:ConnectionId='7' Content="Apply delta"                     />

            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Income cliffs" />
            <TextBlock Opacity="0.7" TextWrapping="Wrap"
                Text="Optional haircut on expected/historical income in Spendable (bad commission months)." />
            <CheckBox x:ConnectionId='8' x:Name="CliffOn" Content="Enable income cliffs" />
            <NumberBox x:ConnectionId='9' x:Name="CliffFactor" Header="Factor (1 = full, 0.5 = half)" Minimum="0" Maximum="1" Value="1" />
            <Button x:ConnectionId='10' Content="Save cliffs"                     />

            <TextBlock x:ConnectionId='11' x:Name="MsgText" Opacity="0.85" TextWrapping="Wrap" />
            <InfoBar x:ConnectionId='12' x:Name="ErrorBar" IsClosable="True" IsOpen="False" Severity="Error" />
        </StackPanel>
    </ScrollViewer>
</Page>

