<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="HonestSpend_WinUI.Pages.RulesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="16" MaxWidth="800">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Category rules" />
                    <TextBlock Opacity="0.75" TextWrapping="Wrap"
                        Text="When payee matches, assign a category. Learned from Sort charges — edit here anytime." />
                </StackPanel>
                <Button x:ConnectionId='17' Grid.Column="1" Content="Refresh"                       />
            </Grid>

            <TextBlock x:ConnectionId='2' x:Name="StatusText" Opacity="0.8" TextWrapping="Wrap" />

            <ListView x:ConnectionId='3' x:Name="RuleList" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,8" Padding="12"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1" CornerRadius="8">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Spacing="2">
                                    <TextBlock FontWeight="SemiBold" Text="{Binding Title}" TextWrapping="Wrap" />
                                    <TextBlock Opacity="0.7" Text="{Binding Subtitle}" TextWrapping="Wrap" />
                                </StackPanel>
                                <Button x:ConnectionId='16' Grid.Column="1" Content="Delete" Tag="{Binding Id}"
                                                             VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Add rule" />
            <TextBox x:ConnectionId='4' x:Name="PatternBox" Header="Payee text to match" PlaceholderText="e.g. AMAZON or NETFLIX"
                                                   />
            <ComboBox x:ConnectionId='5' x:Name="MatchBox" Header="How to match" SelectedIndex="0"                                 >
                <ComboBoxItem Content="Contains (usual)" Tag="contains" />
                <ComboBoxItem Content="Starts with" Tag="starts_with" />
                <ComboBoxItem Content="Exact match" Tag="exact" />
                <ComboBoxItem Content="Regex (advanced)" Tag="regex" />
            </ComboBox>
            <ComboBox x:ConnectionId='6' x:Name="CatBox" Header="Assign category" HorizontalAlignment="Stretch" />
            <NumberBox x:ConnectionId='7' x:Name="PriorityBox" Header="Priority (higher wins if two match)" Value="100" />
            <CheckBox x:ConnectionId='8' x:Name="TransferBox" Content="Treat as transfer (not spend)" />
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button x:ConnectionId='13' Content="Test pattern"                    />
                <Button x:ConnectionId='14' Content="Add rule"                   Style="{StaticResource AccentButtonStyle}" />
            </StackPanel>

            <Border Padding="12" CornerRadius="8"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1">
                <StackPanel Spacing="6">
                    <TextBlock FontWeight="SemiBold" Text="Would match (recent payees)" />
                    <TextBlock x:ConnectionId='11' x:Name="TestSummaryText" Opacity="0.75" TextWrapping="Wrap"
                               Text="Type a pattern and tap Test (or wait a moment)." />
                    <ListView x:ConnectionId='12' x:Name="TestMatchList" SelectionMode="None" MaxHeight="180" />
                </StackPanel>
            </Border>

            <TextBlock x:ConnectionId='9' x:Name="MsgText" Opacity="0.85" />
            <InfoBar x:ConnectionId='10' x:Name="ErrorBar" IsClosable="True" IsOpen="False" Severity="Error" />
        </StackPanel>
    </ScrollViewer>
</Page>

