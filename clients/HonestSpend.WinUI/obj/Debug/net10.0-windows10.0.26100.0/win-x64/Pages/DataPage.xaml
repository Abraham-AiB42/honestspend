<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="HonestSpend_WinUI.Pages.DataPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="16" MaxWidth="800">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Data &amp; backup" />
                    <TextBlock Opacity="0.75" TextWrapping="Wrap"
                        Text="Local SQLite only. Backup to disk, OneDrive-friendly folder, or download ZIP." />
                </StackPanel>
                <Button x:ConnectionId='28' Grid.Column="1" Content="Refresh"                       />
            </Grid>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="12" Padding="16">
                <StackPanel Spacing="6">
                    <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Live database" />
                    <TextBlock x:ConnectionId='25' x:Name="DbPathText" IsTextSelectionEnabled="True" TextWrapping="Wrap" />
                    <TextBlock x:ConnectionId='26' x:Name="DbMetaText" Opacity="0.75" TextWrapping="Wrap" />
                    <TextBlock x:ConnectionId='27' x:Name="EngineText" Opacity="0.7" TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="12" Padding="16">
                <StackPanel Spacing="10">
                    <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Auto-backup schedule" />
                    <TextBlock Opacity="0.7" TextWrapping="Wrap"
                        Text="While the engine is running it checks every ~15 min. Keep copies under ~/.financial-os/backups." />
                    <CheckBox x:ConnectionId='19' x:Name="AutoOnBox" Content="Enable scheduled auto-backup" IsChecked="True" />
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <NumberBox x:ConnectionId='23' x:Name="IntervalBox" Header="Interval (hours)" Minimum="1" Maximum="720" Value="24" />
                        <NumberBox x:ConnectionId='24' x:Name="KeepBox" Header="Keep last N backups" Minimum="1" Maximum="100" Value="14"
                                   Grid.Column="1" />
                    </Grid>
                    <TextBlock x:ConnectionId='20' x:Name="ScheduleMeta" Opacity="0.75" TextWrapping="Wrap" />
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button x:ConnectionId='21' Content="Save schedule"                            Style="{StaticResource AccentButtonStyle}" />
                        <Button x:ConnectionId='22' Content="Run auto-backup now"                       />
                    </StackPanel>
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button x:ConnectionId='16' Content="Create backup"                      Style="{StaticResource AccentButtonStyle}" />
                <Button x:ConnectionId='17' Content="Download live ZIP…"                            />
                <Button x:ConnectionId='18' Content="Restore from file…"                             />
            </StackPanel>
            <TextBox x:ConnectionId='2' x:Name="NoteBox" Header="Backup note (optional)" PlaceholderText="before tax season" />

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="12" Padding="16">
                <StackPanel Spacing="10">
                    <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Encrypted cloud snapshot" />
                    <TextBlock Opacity="0.75" TextWrapping="Wrap"
                        Text="AES-256-GCM (.lrenc). Copy to any folder you choose (OneDrive, Dropbox, etc.). Not a live multi-writer DB." />
                    <TextBox x:ConnectionId='10' x:Name="RemoteFolderBox" Header="Destination folder"
                             PlaceholderText="C:\Users\…\OneDrive\HonestSpend\backups" />
                    <CheckBox x:ConnectionId='11' x:Name="AutoCopyEncBox" Content="Copy encrypted backups to destination" IsChecked="True" />
                    <PasswordBox x:ConnectionId='12' x:Name="EncPassBox" Header="Encryption password (min 8 chars)" />
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button x:ConnectionId='14' Content="Save destination"                          />
                        <Button x:ConnectionId='15' Content="Create encrypted backup"                         Style="{StaticResource AccentButtonStyle}" />
                    </StackPanel>
                    <TextBlock x:ConnectionId='13' x:Name="RemoteHintText" Opacity="0.65" FontSize="12" TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Saved backups" />
            <ListView x:ConnectionId='3' x:Name="BackupList" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,8" Padding="12"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1" CornerRadius="8">
                            <Grid ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Spacing="2">
                                    <TextBlock FontWeight="SemiBold" Text="{Binding Name}" IsTextSelectionEnabled="True" />
                                    <TextBlock Opacity="0.7" Text="{Binding Meta}" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6">
                                    <Button x:ConnectionId='8' Content="Save as…" Tag="{Binding Name}"                             />
                                    <Button x:ConnectionId='9' Content="Restore" Tag="{Binding Name}"                            />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <TextBlock x:ConnectionId='4' x:Name="MsgText" Opacity="0.85" TextWrapping="Wrap" IsTextSelectionEnabled="True" />
            <InfoBar x:ConnectionId='5' x:Name="ErrorBar" IsClosable="True" IsOpen="False" Severity="Error" />
            <InfoBar x:ConnectionId='6' x:Name="WarnBar" IsClosable="True" IsOpen="False" Severity="Warning"
                     Title="Restart recommended after restore" />
        </StackPanel>
    </ScrollViewer>
</Page>

