<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="HonestSpend_WinUI.Pages.TaxPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="24">
        <StackPanel Spacing="16" HorizontalAlignment="Stretch">
            <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Tax packet" />
                    <TextBlock Opacity="0.75" TextWrapping="Wrap"
                        Text="IRS-mapped COA → TurboTax/CPA export. Multi-state notes for CPA — not tax advice." />
                </StackPanel>
                <Button Grid.Column="1" Content="Refresh" Click="Refresh_Click" />
            </Grid>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="12" Padding="16">
                <StackPanel Spacing="8">
                    <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Chart of accounts" />
                    <TextBlock x:Name="CoaText" Opacity="0.85" TextWrapping="Wrap" />
                    <ItemsControl x:Name="CoaForms" />
                </StackPanel>
            </Border>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="12" Padding="16">
                <StackPanel Spacing="12">
                    <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Entity tax geo" />
                    <Grid ColumnSpacing="12" RowSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <ComboBox x:Name="ProfileBox" Header="Entity" HorizontalAlignment="Stretch"
                                  SelectionChanged="Profile_Changed" />
                        <TextBox x:Name="HomeStateBox" Header="Home state (e.g. TX)" Grid.Column="1"
                                 MaxLength="8" />
                        <CheckBox x:Name="MultiStateBox" Content="Multi-state filer" Grid.Row="1" Grid.Column="0" />
                        <TextBox x:Name="AllocBox" Header='State allocation JSON e.g. [{"state":"TX","pct":70}]'
                                 Grid.Row="1" Grid.Column="1" TextWrapping="Wrap" />
                        <TextBox x:Name="NotesBox" Header="Filing notes for CPA" Grid.Row="2" Grid.ColumnSpan="2"
                                 TextWrapping="Wrap" AcceptsReturn="True" Height="64" />
                    </Grid>
                    <Button Content="Save entity tax fields" Click="SaveProfile_Click" Style="{StaticResource AccentButtonStyle}" />
                </StackPanel>
            </Border>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="12" Padding="16">
                <StackPanel Spacing="12">
                    <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Build packet" />
                    <NumberBox x:Name="YearBox" Header="Tax year" Minimum="2000" Maximum="2100"
                               SpinButtonPlacementMode="Inline" />
                    <TextBlock x:Name="ReadyText" FontWeight="SemiBold" TextWrapping="Wrap" />
                    <ItemsControl x:Name="ReadyIssues" />
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button Content="Check readiness" Click="Ready_Click" />
                        <Button Content="Preview" Click="Preview_Click" Style="{StaticResource AccentButtonStyle}" />
                        <Button Content="Save ZIP…" Click="Download_Click" />
                        <Button Content="CPA pack ZIP…" Click="CpaPack_Click" />
                        <Button Content="Write to engine data dir" Click="Write_Click" />
                    </StackPanel>
                    <TextBlock x:Name="PacketMeta" Opacity="0.8" TextWrapping="Wrap" />
                    <TextBlock x:Name="DisclaimerText" Opacity="0.65" FontSize="12" TextWrapping="Wrap" />
                    <TextBlock x:Name="TokenText" Opacity="0.9" FontFamily="Consolas" TextWrapping="Wrap"
                               IsTextSelectionEnabled="True" />
                </StackPanel>
            </Border>

            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="By tax line" />
            <ListView x:Name="LineList" SelectionMode="None" MaxHeight="360">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,0,0,6" Padding="12"
                                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1" CornerRadius="8">
                            <StackPanel Spacing="2">
                                <TextBlock FontWeight="SemiBold" Text="{Binding Title}" TextWrapping="Wrap" />
                                <TextBlock Opacity="0.75" Text="{Binding Subtitle}" TextWrapping="Wrap" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Needs category / nontax" />
            <ItemsControl x:Name="UncatList" />

            <TextBlock x:Name="MsgText" Opacity="0.85" TextWrapping="Wrap" IsTextSelectionEnabled="True" />
            <InfoBar x:Name="ErrorBar" IsClosable="True" IsOpen="False" Severity="Error" />
        </StackPanel>
    </ScrollViewer>
</Page>
